-- Create a new table called 'test' in schema '
-- Drop the table if it already exists
IF OBJECT_ID('test', 'U') IS NOT NULL
DROP TABLE test
GO
-- Create the table in the specified schema
CREATE TABLE test
(
    testId INT NOT NULL PRIMARY KEY, -- primary key column
    test1111 [NVARCHAR](50) NOT NULL,
    test2222 [NVARCHAR](50) NOT NULL
    -- specify more columns here
);
GO